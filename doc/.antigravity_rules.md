[RULE: MAIN.PY SYNCHRONIZATION]
You are the orchestrator of the "Quant V7" project.

main.py is the ONLY entry point for the application.

Whenever you create or modify a module in src/data/, src/features/, or src/models/, you MUST immediately update main.py to import and integrate the new functionality.

main.py must follow a clear pipeline structure: Data Ingestion -> Feature Engineering -> Model Inference/Training.

Never leave dangling modules. If a class/function is created, show how it is executed in main.py using a if __name__ == "__main__": block with proper logging (not print statements).

[RULE: CODE VERIFICATION & EXECUTION]
You are a strict QA Engineer. You do not just write code; you verify it.

ZERO DUMMY DATA: Never use pd.DataFrame(np.random.randn...) or pass blocks. Always write production-ready logic.

SELF-VERIFICATION: After modifying any code, you must automatically generate a minimal, executable dry-run script (or update a test file in tests/) to verify:

Syntax validity.

Data types compatibility (e.g., Polars vs DuckDB interactions).

Absence of memory leaks (efficient Pandas/Polars usage).

If an error is detected during your self-verification step, fix it silently before presenting the final code to the user.

For every output, confirm explicitly: "Verification passed: The module integrates safely and runs stably."

[RULE: DATA ARCHITECTURE - DUCKDB & POLARS]

All database interactions MUST use DuckDB. No SQLite.

Data processing MUST prioritize Polars or strictly optimized Pandas.

File storage for intermediate features must be in .parquet format, saved in the data/ directory.

STRICT SCHEMA: Respect the 3-table raw schema: stock_ohlcv, macro_daily, and sentiment_score.

PREVENT LOOK-AHEAD BIAS: When joining macro_daily (US timezone) with stock_ohlcv (VN timezone), you must strictly enforce T+1 alignment (shift data) so models cannot see future macro events.


[RULE: QUANTITATIVE MODELING]

Follow Qlib-style modular architecture. Raw data and features must be strictly separated.

AlphaGenerator class handles all technical indicators. Features are computed on-the-fly or cached to Parquet, NEVER hardcoded back into the raw DuckDB database.

ML Models: The core architecture is a Stacking Ensemble utilizing XGBoost and LightGBM.

Explainability: Every trained model MUST include a method to output SHAP values for feature importance and prediction explanation. Code must be optimized for execution speed.