# Quant V4 Dashboard — dark-premium native theme.
# Palette mirrors dashboard/theme.py (keep in sync).

[theme]
base = "dark"
primaryColor = "#2dd4a7"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#1a1f2b"
textColor = "#e6e8eb"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
