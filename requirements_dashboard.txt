# Dashboard-only dependencies (additive to requirements.txt).
# Install into the same venv as the main project:
#   pip install -r requirements_dashboard.txt
#
# Packages already pinned in requirements.txt (python-dotenv, pandas, duckdb,
# google-genai, etc.) are intentionally NOT duplicated here.

# 1.39+: st.button(type="tertiary") used for neutral ghost buttons in the theme.
streamlit>=1.39
# Fan-chart forecast (Tầm Nhìn Thuật Toán tab) — go.Scatter + st.plotly_chart.
plotly>=5.18
