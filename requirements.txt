# Quant Engine V4.0 — Python dependencies (PINNED to the known-good env).
# Regenerate after an intentional upgrade, e.g.:
#   pip install -U <pkg> && pip freeze | grep -iE '^(numpy|pandas|...)='
# Python 3.11.
#
# ── Core numerics / data ─────────────────────────────────────────────────────
numpy==2.3.5
pandas==3.0.2
polars==1.40.1                 # V3/V4 feature pipeline is polars-native
scipy==1.16.3
scikit-learn==1.8.0            # ensemble meta + CalibratedClassifierCV (estimator= kwarg needs >=1.2)
# ── Storage ──────────────────────────────────────────────────────────────────
duckdb==1.5.2
pyarrow==24.0.0
# ── Macro data source ────────────────────────────────────────────────────────
yfinance==1.2.2                # DXY / USD-VND / SP500 daily (src/data/macro_crawler.py)
# ── Models — tabular ensemble base learners ──────────────────────────────────
# ALL THREE are required.  tabular_ensemble.py imports them behind try/except
# (graceful degradation), so a MISSING one does NOT error — it silently drops a
# base learner and trains a weaker stack.  Keep all three pinned.
lightgbm==4.6.0
xgboost==3.2.0
catboost==1.2.10
hmmlearn==0.3.3                # Macro Risk Oracle — 2-state Gaussian HMM regime overlay
arch==8.0.0                    # GARCH(1,1) conditional volatility (garch_hmm_regime.py)
# ── Telegram bot + config ────────────────────────────────────────────────────
python-telegram-bot==22.7      # run_bot.py / telegram_bot.py (HARD import — bot won't start without it)
python-dotenv==1.2.2
# ── News / sentiment (arbitrator + sentiment crawler) ────────────────────────
gnews==0.4.3
googlenewsdecoder==0.1.7
beautifulsoup4==4.14.3
google-genai==1.70.0           # official GenAI SDK (replaces the deprecated google-generativeai)
# ── Data / IO ────────────────────────────────────────────────────────────────
aiohttp==3.13.5
requests==2.33.1
# ── Progress / utils ─────────────────────────────────────────────────────────
tqdm==4.67.3
joblib==1.5.3
#
# NOTE: `torch` was intentionally REMOVED.  Its only user — walk_forward.make_lstm_oracle
# — is dead (V4 is pure-tabular; the ensemble oracle needs no torch).  Re-add
# `torch==2.11.0` only if the LSTM oracle is revived.
